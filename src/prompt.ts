// Handles the terminal prompt where the human approves or rejects the rewrite.

export async function askHumanVerdict(): Promise<"approve" | "reject"> {
  // TODO: implement interactive terminal prompt
  return "approve";
}
